export const PLAYBACK_RATES = [0.75, 1, 1.25, 1.5] as const;

/** «Назад»: меньше — предыдущий трек; больше — в начало (двойной тап — предыдущий) */
export const PREV_PREVIOUS_TRACK_MAX_SEC = 10;
export const PREV_DOUBLE_TAP_MS = 450;
